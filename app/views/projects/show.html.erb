<h1><%= @project.name %></h1>

<h2>Votes</h2>
<ul>
  <% @project.votes.each do |vote| %>
    <li><%= vote.user.username %></li>
  <% end %>
</ul>
<%= link_to "vote for this project", [:new, @project, :vote] %>
